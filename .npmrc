# Hexo 及 Butterfly 主题会用 require() 直接加载插件/主题的依赖，
# pnpm 默认的符号链接结构在 Windows 上会导致 "Cannot find module 'hexo-util' / 'moment-timezone'" 之类的解析失败。
# 用 hoisted 让 node_modules 扁平化（类似 npm），保证所有依赖都能被解析。
# Cloudflare Pages 构建时也会读取此文件，保持本地与线上一致。
node-linker=hoisted
